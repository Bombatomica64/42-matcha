export const environment = {
  production: false,
  apiUrl: "https://api.matcha.bombatomica64.dev",
  socketUrl: "https://api.matcha.bombatomica64.dev",
  // Internal URLs for SSR inside Docker if needed
  serverApiUrl: "http://backend:3000",
  serverSocketUrl: "http://backend:3000",
};
